# React + TypeScript + Vite

To run the simulation, first install `npm`, then run the following commands:
```
cd ./web_simulation/
npm install
npm start
```

Live version accessible at: [naru-kami.github.io/xy-model](https://naru-kami.github.io/xy-model/)